# PlatformIO / Arduino

> **TESTED WITH ASTRONVIM**

* TSInstall cpp
* With Mason Installer `install clangd`
* In PlatformIO CLI:
  * `pio project init --ide vim --board <board_name>`
  * `pio run -t compiledb`
